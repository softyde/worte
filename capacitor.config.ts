import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.annee.worte',
  appName: 'Worte',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
